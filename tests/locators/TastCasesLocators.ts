export class TestCasesLocators {

    // Home Page
    homePageText =
        "(//h2[contains(text(),'Full-Fledged practice website for Automation Engineers')])[1]";

    // Test Cases button
    testCasesButton =
        "//a[contains(text(),'Test Cases')]";

    // Test Cases Page
    testCasesPageText =
        "//b[contains(text(),'Test Cases')]";
}